
# 🛠️ DevOps Portfolio: Автоматизація та Інструментарій

Це репозиторій, що містить набір DevOps інструментів, розроблених для автоматизації робочого процесу: резервне копіювання, моніторинг, аналіз логів та організація файлів. Всі інструменти інтегровані через **Git Hooks** для забезпечення якості коду та автоматичного виконання завдань.

### Компіляція Go CLI інструменту 

Перейдіть до директорії `go-cli` та скомпілюйте виконуваний файл:

```bash
cd devops-portfolio/go-cli
go build -o fileorganizer.exe
cd ../..
```

### Встановлення Git Hooks

Запустіть скрипт для інтеграції робочих хуків (`pre-commit`, `pre-push`, `post-commit`) у ваш репозиторій:

```bash
bash scripts/install-hooks.sh
```

-----

## 🚀 Інструменти та Інструкції з Використання

### I. Bash Скрипти

| Скрипт | Опис | Використання |
| :--- | :--- | :--- |
| **`backup.sh`** | Створює стиснений архів (`tar.gz`), логує процес та очищає старі бекапи (зберігає останні 5). | `bash scripts/backup.sh <Папка_Джерело> <Папка_Призначення>` |
| **`monitor.sh`** | Перевіряє використання CPU, RAM та дискового простору. Надсилає алерт у Discord при перевищенні порогів. | `bash scripts/monitor.sh` (Потрібно налаштувати URL Webhook у скрипті) |

### II. Python Автоматизація

| Скрипт | Опис | Використання |
| :--- | :--- | :--- |
| **`log_parser.py`** | Аналізує веб-сервер логи, підраховує IP, User-Agent, HTTP статус-коди та генерує **HTML-звіт**. | `python scripts/log_parser.py <файл.log> -o <звіт.html>` |
| **`api_monitor.py`** | Перевіряє доступність API endpoints, вимірює response time. Сповіщає про збої через Slack/Discord. | `python scripts/api_monitor.py` (Потрібна конфігурація endpoints/Webhook у скрипті) |

### III. Go CLI: Організатор Файлів

**Назва:** `fileorganizer.exe`

Інструмент, розроблений на Go, для сортування файлів за їхнім типом (розширенням) та переміщенням у відповідні папки (`images/`, `documents/`, `archives/`).

| Параметр | Опис | Приклад |
| :--- | :--- | :--- |
| `-src` | **Обов'язково.** Шлях до директорії, яку потрібно організувати. | `./test_files` |
| `-mode` | Режим роботи: `dryrun` (перевірка) або `move` (переміщення). | `dryrun` / `move` |

**Приклад використання (з кореня репозиторію):**

```bash
# Режим сухого запуску (безпечна перевірка)
./devops-portfolio/go-cli/fileorganizer.exe -src ./test_files -mode dryrun

# Режим переміщення (виконання сортування)
./devops-portfolio/go-cli/fileorganizer.exe -src ./test_files -mode move
```

-----

## 🚦 Тестування Git Hooks

Git Hooks забезпечують автоматизацію якості та завдань при виконанні стандартних Git-операцій.

### 1\. `pre-commit` (Перевірка якості коду)

  * **Дія:** Автоматично запускає лінтер (або перевірку синтаксису) на змінених файлах перед фіксацією змін.
  * **Як протестувати:** Внесіть синтаксичну помилку в `.py` або `.go` файл і спробуйте зробити `git commit`. Коміт має бути **заблокований**.

### 2\. `pre-push` (Виконання тестів)

  * **Дія:** Запускає всі модульні/інтеграційні тести проєкту перед відправкою коду на віддалений репозиторій.
  * **Як протестувати:** Спробуйте `git push`. Якщо тести не пройдуть, push буде **заблокований**.

### 3\. `post-commit` (Автоматичний Backup)

  * **Дія:** Автоматично викликає скрипт `backup.sh` після успішної фіксації змін.
  * **Як протестувати:** Зробіть успішний `git commit`. Вивід `backup.sh` має з'явитися в консолі після повідомлення про коміт, підтверджуючи створення нового бекапу.